<%= simple_form_for(@task) do |f| %>
  <%= f.input :title %>
  <%= f.input :details %>
  <%= f.input :species, as: :select, collection: Task::SPECIES %>
  <% unless @task.id.nil? %>
    <%= f.input :completed, type: 'checkbox' %>
  <% end %>
  <%= f.submit %>
<% end %>
